import { Meta } from '@storybook/addon-docs';

<Meta title="Context/CartList/Documentation" />

# Docs for CartList

CartList leverages `SampleContext` via `useSampleContext()` to render the list of cart items therefrom.

It expects `SampleContext.state` to contain a list of items with `itemID` and `quantity` properties.
