import { Meta } from '@storybook/addon-docs';

<Meta title="Greeter/Documentation" />

# Docs for Greeter

Greeter accepts the props `name`, `profession` and `age`, and renders them in a paragraph in a sentence.
