// .storybook/manager.js
import { addons } from '@storybook/addons';
import { themes } from '@storybook/theming';

// You can customize Storybook's own UI theme here if needed
addons.setConfig({
    theme: themes.normal,
});